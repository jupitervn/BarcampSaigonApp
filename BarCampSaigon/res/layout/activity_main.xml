<org.barcampsaigon.android.ui.CustomizedTabHost xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:hungrygowhere="http://schemas.android.com/apk/res-auto"
    xmlns:appiphany="http://schemas.android.com/res-auto"
    android:id="@android:id/tabhost"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    appiphany:isTabBelow="true" >

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
         >

        <TabWidget
            android:id="@android:id/tabs"
            android:layout_width="match_parent"
            android:layout_height="@dimen/tab_height"
            android:layout_alignParentBottom="true"
            android:background="@android:color/transparent" 
            android:visibility="gone"
            >
        </TabWidget>

        <FrameLayout
            android:id="@android:id/tabcontent"
            android:layout_width="0dp"
            android:layout_height="0dp"
             >
        </FrameLayout>

        <FrameLayout
            android:id="@+id/real_tab_content_id"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_above="@android:id/tabs"
             >
        </FrameLayout>
    </RelativeLayout>

</org.barcampsaigon.android.ui.CustomizedTabHost>